import React from 'react';
import { EdgeConnection } from '../../reducer/structdata';

interface ShowComponentProp {
  connection: EdgeConnection;
}

export default function ShowComponent({ connection }: ShowComponentProp) {}
